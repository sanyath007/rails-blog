<%= form_with(model: [
  @article,
  @article.comments.build
], local: true) do |form| %>
  <div>
    <%= form.label :commenter %><br>
    <%= form.text_field :commenter %>
  </div>
  <div>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </div>

  <%= form.submit %>
<% end %>